# Python Agent Identity SDK

Utilities included:
- Build signed agent request tokens (EdDSA).
- Build signed handshake request tokens.
- Compute deterministic body hash helper.

This MVP package expects caller-managed key material.
